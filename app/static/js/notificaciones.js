"use strict";
const apiNotificaciones = document.body.dataset.api, csrfNotificaciones = document.querySelector('meta[name="csrf-token"]').content;
async function pedirNotificacion(url, opciones = {}) { const respuesta = await fetch(url, { ...opciones, headers: { "X-CSRFToken": csrfNotificaciones, Accept: "application/json" } }); const datos = await respuesta.json(); if (!respuesta.ok) throw new Error(datos.mensaje || "No fue posible completar la operación"); return datos; }
async function cargarNotificaciones() { const datos = await pedirNotificacion(apiNotificaciones), lista = document.getElementById("lista"); lista.replaceChildren(); for (const notificacion of datos.notificaciones || []) { const tarjeta = document.createElement("article"); tarjeta.className = "tarjeta"; const fecha = document.createElement("small"); fecha.textContent = new Date(notificacion.creado_en).toLocaleString("es-CL"); const titulo = document.createElement("h2"); titulo.textContent = notificacion.titulo; const mensaje = document.createElement("p"); mensaje.textContent = notificacion.mensaje; tarjeta.append(fecha, titulo, mensaje); if (notificacion.leida) tarjeta.append(document.createTextNode("Leída")); else { const boton = document.createElement("button"); boton.type = "button"; boton.dataset.id = String(notificacion.id); boton.textContent = "Marcar leída"; tarjeta.append(boton); } lista.append(tarjeta); } if (!(datos.notificaciones || []).length) { const vacio = document.createElement("p"); vacio.className = "tarjeta"; vacio.textContent = "No tienes notificaciones."; lista.append(vacio); } }
document.getElementById("lista").addEventListener("click", async (evento) => { const boton = evento.target.closest("button[data-id]"); if (!boton) return; await pedirNotificacion(`${apiNotificaciones}/${boton.dataset.id}/leer`, { method: "POST" }); await cargarNotificaciones(); });
document.getElementById("todas").addEventListener("click", async () => { await pedirNotificacion(`${apiNotificaciones}/leer-todas`, { method: "POST" }); await cargarNotificaciones(); });
cargarNotificaciones();
